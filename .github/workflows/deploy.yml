# ⚠️ DEPRECATED — use astro-pages.yml instead
# This workflow is no longer active.
# All deployment configuration has been consolidated into .github/workflows/astro-pages.yml
# which includes PUBLIC_ANALYTICS_API env variable support.
#
# To restore this workflow, remove this banner and re-enable in GitHub Actions.

name: Deploy to GitHub Pages (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use astro-pages.yml instead."
